---
services:
  php:
    build:
      context: .
      args:
        PHP_VERSION: "${PHP_VERSION:-8.2}"
    restart: no
    env_file:
      - ./.env
    volumes:
      - ..:/web
    working_dir: /web
